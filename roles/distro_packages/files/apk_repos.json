{
    "$schema": "http://json-schema.org/draft-04/schema#",
    "type": "object",
    "description": "Schema to validate the content of the distro_packages_repos var for apk",
    "properties": {
        "url": {
            "type": "string",
            "description": "URL of the repository to add."
        }
    },
    "required": [
        "url"
    ]
}
